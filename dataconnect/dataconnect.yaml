specVersion: "v1beta"
serviceId: "student-attendance-system"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "student-attendance-system-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
